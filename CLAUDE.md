# 執筆ルール

## 対象読者

プログラミングまったくの初心者。

---

## 章・節のタイトル命名規則

| レベル | ルール | 例 |
|--------|--------|-----|
| Chapter | 名詞形 | `Chapter 4：PythonとDjango基礎` |
| x-x（節） | 名詞形 | `4-1 Djangoのプロジェクト構造` |
| 箇条書き（概念説明） | 名詞形または体言止め | `プロジェクトとアプリの違い` |
| 箇条書き（ハンズオン） | 「～する」形 + 🛠️ | `🛠️ スーパーユーザーを作成する` |

---

## 🛠️ アイコンのルール

- **実際に手を動かす箇所**（コードを書く・コマンドを実行する・設定を変更する等）にのみ付ける
- 概念の説明・図の確認・読むだけの箇所には付けない
- 箇条書きの行頭に付ける（節タイトルには付けない）
- 🛠️ が付いている行は必ず「～する」形で書く

---

## 構文説明の初出ルール

Python・JavaScript・TypeScriptの構文は、**独立した文法章を設けず、実際のソースコードの中で初出時に説明する**。

### ルール

- **初出時**：コードの該当箇所にコメントまたは本文で構文の説明を入れる
- **2回目以降**：同じ構文の説明は繰り返さない
- 説明は簡潔に。「何をするものか」だけを伝え、言語仕様の網羅的な説明はしない

### 例

```python
# 初出時：def が初めて登場する箇所で説明する
def get_books(request):   # def は関数の定義。処理に名前をつけてまとめる
    books = Book.objects.all()
    return Response(...)

# 2回目以降：説明なし
def create_book(request):
    ...
```

### 対象となる主な構文

| 言語 | 初出時に説明する構文 |
|------|-------------------|
| Python | def・class・self・リスト・dict・for・if・型ヒント |
| TypeScript | const・let・アロー関数・interface・型注釈・async/await・map/filter |

---

## コードの省略ルール

- 原則として**コードの省略（`# ...` や `略` 等）は使わない**
- ファイルが肥大化してどうしても必要な場合のみ省略を使う
- 省略する場合は「前章からの変更箇所のみ抜粋」等、理由を明記する

---

## ファイル構成

原稿ファイルは `chapters/` ディレクトリに置く。1章につき1ファイル。

| 種別 | パス | 命名規則 | 例 |
|------|------|---------|-----|
| 目次・章立て | ルート | `README.md` | `README.md` |
| はじめに | `chapters/` | `00_はじめに.md` | `chapters/00_はじめに.md` |
| 各章 | `chapters/` | `{2桁連番}_{章タイトル}.md` | `chapters/01_Webシステムの仕組み.md` |
| おわりに | `chapters/` | `15_おわりに.md` | `chapters/15_おわりに.md` |

### Markdownの見出しレベル

| 見出し | 用途 |
|--------|------|
| `#` | ファイルのタイトル（Chapter名 or はじめに・おわりに） |
| `##` | x-x 節（例：`## 1-1 URLとリクエストの流れ`） |
| `###` | 節内の小見出し（必要な場合のみ） |

---

## 技術スタック

| 項目 | 内容 |
|------|------|
| フロントエンド | Next.js（TypeScript） |
| バックエンド | Django（Python） |
| DB | MySQL系 |
| 開発環境 | VSCode + DevContainer（1ウィンドウ） |
| コンテナ構成 | Python + Node.js を1つのDevContainer、MySQLはdocker-compose |
| リポジトリ | モノレポ |
| アプリ | 図書館貸出システム |
