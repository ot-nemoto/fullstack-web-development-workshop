# Chapter 12：テスト

この章では、作ったシステムにテストを書きます。テストコードを書くことで、「変更を加えたときに既存の機能が壊れていないか」を自動的に確認できるようになります。

## 13-1 テストの目的と種類

### なぜテストを書くのか

テストなしで開発を続けると、次のような問題が起きます。

- ある機能を修正したら、別の機能が壊れた
- リリース直前に重大なバグを発見した
- 「この処理は本当に正しく動いているか」を手で確認するのに時間がかかる

テストコードがあると、変更のたびにコマンド1つで全機能の動作確認ができます。「どこを変えても安心して変えられる」という状態が、開発を速くする土台になります。

### ユニットテスト・結合テスト・E2Eテストの違い

| 種類 | 何をテストするか | 速度 | 信頼性 |
|------|--------------|------|--------|
| **ユニットテスト** | 関数・クラス単体 | 速い | 個々の処理が正しいか |
| **結合テスト** | 複数の処理の連携（APIなど） | 中程度 | 層をまたいで正しく動くか |
| **E2Eテスト** | ブラウザの操作全体 | 遅い | ユーザー操作として正しいか |

### テストピラミッドの概念

```
        /  E2E  \       ← 少数（遅い・コストが高い）
       /  結合テスト \
      / ユニットテスト \  ← 多数（速い・書きやすい）
```

テストの数はユニットテストを多く、E2Eを少なくするのが効率的です。本書では Django の結合テスト（APIテスト）と境界値テスト、Next.js のコンポーネントテスト（ユニットテスト）を書きます。

## 13-2 Djangoのテスト

### 🛠️ TestCaseの書き方を理解する

Djangoのテストは `TestCase` クラスを継承して書きます。`backend/library/tests.py` を開きましょう。

```python
from django.test import TestCase               # Django組み込みのテストクラス
from django.contrib.auth.models import User
from rest_framework.test import APIClient      # DRFのAPIテスト用クライアント
from rest_framework import status              # HTTPステータスコードの定数
from .models import Book, Category, Loan
from datetime import date, timedelta


class BookAPITestCase(TestCase):               # TestCaseを継承してテストクラスを定義する

    def setUp(self):                           # setUp は各テストメソッドの前に必ず実行される
        self.client = APIClient()
        self.user = User.objects.create_user(  # テスト用ユーザーを作成する
            username='testuser',
            password='testpass123',
        )
        self.category = Category.objects.create(name='プログラミング')
        self.book = Book.objects.create(
            title='テスト本',
            author='テスト著者',
            publisher='テスト出版社',
            isbn='9784000000001',
            category=self.category,
            available_count=3,
        )
```

テストメソッドは `test_` で始まる名前にします。Djangoがこのプレフィックスを見てテストを自動検出します。

### 🛠️ モデルのテストを書く

```python
class BookModelTestCase(TestCase):

    def setUp(self):
        self.category = Category.objects.create(name='プログラミング')

    def test_book_str_returns_title(self):       # __str__ が正しく動くか
        book = Book.objects.create(
            title='テスト本',
            author='テスト著者',
            publisher='テスト出版社',
            isbn='9784000000001',
            category=self.category,
            available_count=1,
        )
        self.assertEqual(str(book), 'テスト本')  # assertEqual は2つの値が等しいか確認する

    def test_book_default_available_count(self):  # デフォルト値が正しいか
        book = Book.objects.create(
            title='テスト本2',
            author='テスト著者',
            publisher='テスト出版社',
            isbn='9784000000002',
        )
        self.assertEqual(book.available_count, 1)
```

### 🛠️ APIのテストを書く（APIClient）

```python
class BookAPITestCase(TestCase):

    def setUp(self):
        self.client = APIClient()
        self.user = User.objects.create_user(username='testuser', password='testpass123')
        self.category = Category.objects.create(name='プログラミング')
        self.book = Book.objects.create(
            title='テスト本',
            author='テスト著者',
            publisher='テスト出版社',
            isbn='9784000000001',
            category=self.category,
            available_count=3,
        )

    def test_get_book_list(self):               # 一覧取得は認証なしで取得できる
        response = self.client.get('/api/books/')
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.assertEqual(len(response.data), 1) # 1件登録したので1件返ってくる

    def test_get_book_detail(self):
        response = self.client.get(f'/api/books/{self.book.id}/')
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.assertEqual(response.data['title'], 'テスト本')

    def test_create_book_requires_auth(self):   # 未認証では登録できない
        response = self.client.post('/api/books/', {
            'title': '新しい本',
            'author': '著者',
            'publisher': '出版社',
            'isbn': '9784000000099',
            'available_count': 1,
        })
        self.assertEqual(response.status_code, status.HTTP_401_UNAUTHORIZED)

    def test_create_book_with_auth(self):       # 認証済みなら登録できる
        self.client.force_authenticate(user=self.user)
        # テストでは「APIの動作を確認すること」が目的であり、ログインフローを毎回実行するのは遅く本題と関係がない
        # force_authenticate は本物のトークン発行を省略して「このユーザーで認証済み」の状態を強制的に作る
        response = self.client.post('/api/books/', {
            'title': '新しい本',
            'author': '著者',
            'publisher': '出版社',
            'isbn': '9784000000099',
            'available_count': 1,
        })
        self.assertEqual(response.status_code, status.HTTP_201_CREATED)
        self.assertEqual(Book.objects.count(), 2)  # 元の1件 + 新規1件 = 2件
```

### 🛠️ テストを実行する

```bash
python manage.py test
```

```
Found 5 test(s).
Creating test database for alias 'default'...
System check identified no issues (0 silenced).
.....
----------------------------------------------------------------------
Ran 5 tests in 0.312s

OK
Destroying test database for alias 'default'...
```

すべてのテストが通ると `OK` と表示されます。Djangoはテスト実行のたびに専用のテスト用データベースを自動作成・削除するため、本番のデータに影響しません。

特定のファイルやクラスだけを実行する場合：

```bash
python manage.py test library.tests.BookAPITestCase
```

## 13-3 境界値テスト

### 正常系・異常系の考え方

**正常系**は「正しい入力をしたとき、正しい結果が返る」テストです。**異常系**は「不正な入力や想定外の状態のとき、適切にエラーになる」テストです。

図書館貸出システムのビジネスロジックには境界値が自然に発生します。「貸出可能数が0のとき」「ちょうど0になる1冊目の貸出時」「すでに借りている本をもう一度借りようとしたとき」といった境界をテストすることで、ロジックの穴を発見できます。

### 🛠️ 貸出可能数が0のときの貸出拒否テストを書く

```python
class LoanBoundaryTestCase(TestCase):

    def setUp(self):
        self.client = APIClient()
        self.user = User.objects.create_user(username='borrower', password='testpass123')
        self.client.force_authenticate(user=self.user)
        self.category = Category.objects.create(name='テスト')

    def _create_book(self, available_count):    # テスト用ヘルパーメソッド（_で始める慣例）
        return Book.objects.create(
            title='テスト本',
            author='テスト著者',
            publisher='テスト出版社',
            isbn=f'978400000{available_count:04d}',  # available_countを末尾に使って重複を避ける
            category=self.category,
            available_count=available_count,
        )

    def _loan_request(self, book_id):
        due_date = (date.today() + timedelta(days=14)).isoformat()
        return self.client.post('/api/loans/', {
            'book': book_id,
            'due_date': due_date,
        })

    def test_loan_rejected_when_available_count_is_zero(self):
        book = self._create_book(available_count=0)
        response = self._loan_request(book.id)
        self.assertEqual(response.status_code, status.HTTP_400_BAD_REQUEST)
        self.assertIn('book', response.data)    # エラーメッセージがbookフィールドに入っているか
```

### 🛠️ 貸出可能数を超えた場合のテストを書く

```python
    def test_available_count_decreases_on_loan(self):
        book = self._create_book(available_count=2)
        self._loan_request(book.id)
        book.refresh_from_db()                  # DBから最新の値を読み直す
        self.assertEqual(book.available_count, 1)

    def test_loan_rejected_when_last_copy_already_loaned(self):
        book = self._create_book(available_count=1)
        other_user = User.objects.create_user(username='other', password='pass123')

        # 他のユーザーが先に借りる
        other_client = APIClient()
        other_client.force_authenticate(user=other_user)
        due_date = (date.today() + timedelta(days=14)).isoformat()
        other_client.post('/api/loans/', {'book': book.id, 'due_date': due_date})

        # available_count が 0 になっているため、自分が借りようとするとエラーになる
        response = self._loan_request(book.id)
        self.assertEqual(response.status_code, status.HTTP_400_BAD_REQUEST)
```

### 🛠️ 同じユーザーが同じ本を重複して借りる場合のテストを書く

```python
    def test_duplicate_loan_is_rejected(self):
        book = self._create_book(available_count=3)
        self._loan_request(book.id)             # 1回目：成功するはず

        response = self._loan_request(book.id)  # 2回目：同じ本を再度借りようとする
        self.assertEqual(response.status_code, status.HTTP_400_BAD_REQUEST)
        self.assertIn('book', response.data)
```

### 🛠️ 存在しない本IDを指定した場合のテストを書く

```python
    def test_loan_nonexistent_book(self):
        due_date = (date.today() + timedelta(days=14)).isoformat()
        response = self.client.post('/api/loans/', {
            'book': 99999,                      # 存在しないID
            'due_date': due_date,
        })
        self.assertEqual(response.status_code, status.HTTP_400_BAD_REQUEST)
```

境界値テストをすべて実行します。

```bash
python manage.py test library.tests.LoanBoundaryTestCase
```

```
Found 4 test(s).
....
----------------------------------------------------------------------
Ran 4 tests in 0.089s

OK
```

全テストをまとめて確認します。

```bash
python manage.py test
```

## 13-4 Next.jsのコンポーネントテスト

### 🛠️ Jestをセットアップする

Next.jsのコンポーネントテストには Jest と Testing Library を使います。

```bash
cd frontend
npm install --save-dev jest jest-environment-jsdom ts-node @testing-library/react @testing-library/jest-dom @types/jest
```

`frontend/jest.config.ts` を作成します。

```typescript
import type { Config } from 'jest'
import nextJest from 'next/jest.js'

const createJestConfig = nextJest({ dir: './' })

const config: Config = {
    testEnvironment: 'jsdom',            // ブラウザ相当のDOM環境でテストを実行する
    setupFilesAfterEnv: ['<rootDir>/jest.setup.ts'],  // テスト環境セットアップ後に読み込むファイル
}

export default createJestConfig(config)
```

`frontend/jest.setup.ts` を作成します。

```typescript
import '@testing-library/jest-dom'      // toBeInTheDocument などのマッチャーを有効にする
```

`frontend/package.json` の `scripts` にテスト実行コマンドを追加します。

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "test": "jest"
  }
}
```

### 🛠️ Testing Libraryでコンポーネントテストを書く（1例）

`frontend/src/components/__tests__/BookCard.test.tsx` を作成します。

```tsx
import { render, screen } from '@testing-library/react'
// render: コンポーネントをテスト用DOMにレンダリングする
// screen: レンダリング結果からDOM要素を検索する
import BookCard from '../BookCard'
import { Book } from '@/types'

const mockBook: Book = {
    id: 1,
    title: 'テスト本のタイトル',
    author: 'テスト著者',
    publisher: 'テスト出版社',
    isbn: '9784000000001',
    category: 1,
    available_count: 3,
}

describe('BookCard', () => {    // describe はテストのグループをまとめる

    it('タイトルと著者名が表示される', () => {
        render(<BookCard book={mockBook} />)
        expect(screen.getByText('テスト本のタイトル')).toBeInTheDocument()
        // expect(...).toBeInTheDocument() はその要素がDOMに存在するか検証する
        expect(screen.getByText('テスト著者')).toBeInTheDocument()
    })

    it('貸出可能冊数が1以上のとき「貸出可能」と表示される', () => {
        render(<BookCard book={mockBook} />)
        expect(screen.getByText(/貸出可能/)).toBeInTheDocument()
        // /貸出可能/ は正規表現：「貸出可能」を含むテキストにマッチする
    })

    it('貸出可能冊数が0のとき「貸出中」と表示される', () => {
        render(<BookCard book={{ ...mockBook, available_count: 0 }} />)
        // スプレッド構文 {...mockBook} でオブジェクトを展開し、available_count だけ上書きする
        expect(screen.getByText('貸出中')).toBeInTheDocument()
    })
})
```

テストを実行します。

```bash
npm test
```

```
 PASS  src/components/__tests__/BookCard.test.tsx
  BookCard
    ✓ タイトルと著者名が表示される (23 ms)
    ✓ 貸出可能冊数が1以上のとき「貸出可能」と表示される (5 ms)
    ✓ 貸出可能冊数が0のとき「貸出中」と表示される (4 ms)

Test Suites: 1 passed, 1 total
Tests:       3 passed, 3 total
```

## まとめ

- テストを書く目的（変更に対する安心感・バグの早期発見）と、テストの種類を理解した
- Django の `TestCase` と `APIClient` でモデルテスト・APIテストを書いて実行した
- 貸出可能数ゼロ・重複貸出・存在しないIDといった境界値テストでビジネスロジックを検証した
- Testing Library で Next.js コンポーネントの表示ロジックをテストした

---

次の章では、書いたテストをプッシュするたびに自動で実行する CI/CD を構築します。
